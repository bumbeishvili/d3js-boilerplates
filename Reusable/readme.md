## sample invokation

```javascript
var chart = getChart(config)
                .width(400)
                .height(400)
                
  d3.select(selector)
    .call(chart);
    
    
    
    // OPTIONAL _ update chart
    chart.data(newData)
  

```
